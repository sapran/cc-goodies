{
  "name": "cc-goodies",
  "owner": {
    "name": "Volodymyr Styran",
    "url": "https://github.com/sapran"
  },
  "metadata": {
    "description": "Developer-experience extras for Claude Code: spoken notifications, an enriched statusline, a protected-branch guard, a dangerous-command guard, and an RTK token-optimizer hook.",
    "version": "0.1.0",
    "repository": "https://github.com/sapran/cc-goodies"
  },
  "plugins": [
    {
      "name": "voice-notify",
      "source": "./plugins/voice-notify",
      "description": "Spoken macOS notifications (via `say`) when Claude needs your attention or finishes a turn. Rotating first-person phrases, configurable voice."
    },
    {
      "name": "statusline",
      "source": "./plugins/statusline",
      "description": "Enriched two-line statusline: user@host, cwd, branch/worktree, task focus, model, effort, context and rate-limit usage."
    },
    {
      "name": "git-guard",
      "source": "./plugins/git-guard",
      "description": "Block accidental commits, merges and pushes to protected branches (main/master) before they run. Selectable policies, configurable branch lists."
    },
    {
      "name": "shell-guard",
      "source": "./plugins/shell-guard",
      "description": "Block a curated set of catastrophic shell commands (rm -rf / or ~, dd to a disk device, mkfs/wipefs, fork bombs, curl|sh) before they run. Defence in depth over the static deny list; fail-open, configurable."
    },
    {
      "name": "rtk-hook",
      "source": "./plugins/rtk-hook",
      "description": "Wire the RTK (Rust Token Killer) token-optimizer as a managed PreToolUse hook on the Bash tool. No-ops gracefully when rtk isn't installed."
    }
  ]
}
