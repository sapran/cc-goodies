## 1. SKILL.md — router rewrite (`skills/session-finalise/SKILL.md`)

- [x] 1.1 Replace the "Run the phases in the order below (the order is a safety property)" framing (current line 34) with the stated invariant — nothing is destroyed or discarded before it is saved and confirmed — as the single ordering rule; keep phase names as short pointers, not a numbered script the model must follow verbatim. Done: "How to run" states the invariant once and drops phase numbering (headers are now plain names, not `### N.`).
- [x] 1.2 Strip phase mechanics out of the body: the bash snapshot block, the memory frontmatter/path/index recital (lines 72–78), the tracker vendor-list scan (lines 90–91) and the Asana/`gh` rules (lines 102–103), the cleanup worktree/Claude-Code-project-dir procedure. Each moves to `references/` (task 2) or is dropped outright (tasks 3–5); none stays duplicated in SKILL.md. Done: bash block → `references/orient.md`; memory schema/path/slug/index → dropped outright; tracker vendor list → rewritten capability description; Asana/`gh` rules → dropped; cleanup procedure → `references/cleanup.md`.
- [x] 1.3 Remove `## Common mistakes` (lines 131–138) — the defensive-guardrail anti-pattern; do not replace it with an equivalent list elsewhere in the plugin. Done: section removed, no equivalent added.
- [x] 1.4 Remove `## Quick reference` (lines 140–151) — the in-file duplicate of the phase/gate contract. Confirm no other section re-renders the same table. Done: table removed; grepped whole plugin tree for `| Phase | Mutates` — no hits.
- [x] 1.5 Re-verify `## Safety gates` (lines 123–129) survives intact or is folded into the invariant statement without losing any gate: confirm-before-delete/commit/push/worktree-removal, never `main`, credential handling, "don't invent work when preconditions don't hold." Done: section kept verbatim, all four gates present.

## 2. `references/` — phase detail extraction

- [x] 2.1 Create `skills/session-finalise/references/` with one file per phase (or grouped where a phase is thin), holding exactly the mechanics removed from SKILL.md in task 1.2: commit/stash procedure, handoff delegation note, tracker detection + mutation flow, cleanup procedure (including the worktree + Claude Code project dir removal rule). Done: created `orient.md`, `commit.md`, `memory.md`, `handoff.md`, `trackers.md`, `cleanup.md`.
- [x] 2.2 Grep the finished SKILL.md against every new reference file to confirm no mechanic is present in both places — progressive disclosure must be real, not nominal (the failure mode `project-scope`'s `references/` already has, per the architecture memo point 3). Done: grepped for the bash snapshot commands, the `<worktree-path-slug>` pattern, and the memory schema/index conventions — each appears only in its reference file, not in SKILL.md.
- [x] 2.3 In SKILL.md, name concretely when each reference applies (e.g. "before a tracker mutation, read `references/trackers.md`") rather than a generic "see references/" pointer, so the split has a reason to be read rather than skipped. Done: each phase blurb in SKILL.md names its reference file and states what to read it for.

## 3. Memory phase — reconciliation over schema recital

- [x] 3.1 Rewrite phase 3 as reconciliation: read `MEMORY.md`, judge what this session established that no file records, and what recorded memory is now stale given what's observed on disk. Do not restate the `~/.claude/projects/<slug>/memory/` path, the `/` → `-` derivation rule, the `name`/`description`/`metadata.type` (`user|feedback|project|reference`) frontmatter schema, or the `MEMORY.md` index convention — the harness supplies all of it natively. Done in `references/memory.md`; e2e run confirmed a seeded stale fact was correctly flagged.
- [x] 3.2 Keep the one piece of the old phase that isn't harness-native duplication: "read an existing file in the project's `memory/` dir first and copy its exact frontmatter shape" (line 72) as the bootstrap for the (harness-agnostic) case where no memory file yet exists for a given fact — reconciliation still needs this fallback. Done: kept as the "Fallback for authoring a new file" paragraph.
- [x] 3.3 Confirm "skip anything already authoritative in code or git history" (line 79) is preserved — it's a real judgment rule, not schema duplication. Done: kept verbatim as the closing line of `references/memory.md`.

## 4. Tracker detection — capability framing, not vendor enumeration

- [x] 4.1 Rewrite the "Available tools" detection step (lines 90–91) to describe the capability sought — "a task-tracker tool or CLI is reachable in this session" — rather than a fixed substring list (`asana`, `github`, `linear`, `jira`, `gitlab`, `notion`, `clickup`). Judge a tool by what it claims to do, not by matching its name against an enumerated set. Done in `references/trackers.md`.
- [x] 4.2 Keep the rest of phase 5's structure unchanged in effect: project-config check (`.mcp.json`, `enabledPlugins`), CLI check (`command -v gh`, etc.), the wired/not-wired branch, and "confirm before each mutating call." Done: all four preserved in `references/trackers.md`.
- [x] 4.3 Confirm the rewritten detection step doesn't newly false-positive on an unrelated tool that merely mentions "task" or "issue" — a capability description needs enough specificity (reads/writes items with status, comments, or links) to stay precise without becoming a vendor list again. Done: detection is framed as "read, comment on, label, or change the status of a tracked item," with an explicit non-example ("merely mentions 'task' or 'issue' ... doesn't [count]").

## 5. Remove user-specific product rules

- [x] 5.1 Delete "Asana edits contain no HTML" and "when both a GitHub MCP and `gh` are available, prefer `gh`" (lines 102–103) outright — do not relocate them into `references/` or the README. They stay in the user's own `~/.claude/CLAUDE.md`, where the Asana rule already lives. Done: not present anywhere in the restructured plugin.
- [x] 5.2 Grep the finished plugin tree (`SKILL.md`, `references/`, `README.md`, `commands/session-finalise.md`) for residual mentions of Asana-specific or `gh`-preference wording to confirm none survived the move. Done: grep for "no html" / "prefer `gh`" wording returned zero hits; the only remaining "Asana" mention is the pre-existing, untouched frontmatter trigger-description example ("tracker tasks (GitHub/Asana/Linear/Jira/etc.)"), which is a trigger example, not a product rule, and frontmatter was out of scope for this change.

## 6. Final report — checkable rubric

- [x] 6.1 Replace phase 8's "terse summary: what each phase did, and what was skipped" (lines 119–121) with a rubric a verification pass can apply mechanically: for each phase that was proposed, state whether it ran, what it changed (or why it was skipped), and whether every mutation in it was confirmed before it executed. Keep it terse in tone; make it checkable in structure. Done: "Final report" section in SKILL.md now states the three-part rubric (ran?, changed/why skipped?, mutations confirmed?).

## 7. Orient (phase 1) — subagent-delegable

- [x] 7.1 State in SKILL.md (or the relevant reference) that Orient — a read-only survey (`git status`, `git worktree list`, `git log`, file/tracker-ID scanning) — is subagent-delegable, so its noisy output doesn't have to sit in the main context; a subagent can return a small structured summary instead. Done: "Delegable to a subagent" section in `references/orient.md`.
- [x] 7.2 Leave the choice of whether to actually delegate it to judgment (session size, how noisy the read is) rather than mandating a subagent every run — an unconditional rule here would repeat the over-specification this proposal is removing elsewhere. Done: phrased as "a judgment call ... not a fixed rule to always or never delegate it."

## 8. Docs — README, command, manifest

- [x] 8.1 `README.md`: remove the duplicated phase/gate table (lines 26–35); point at the skill for the phase list instead of restating it. Leave Install, Uninstall, Requirements, and License sections as-is (already accurate, no user-specific content, no external state). Done; those four sections untouched.
- [x] 8.2 `commands/session-finalise.md`: review "Follow that skill's phased checklist exactly" (line 8) — reword so it doesn't imply a rigid enumerated script the restructure just removed; it should point at the invariant-first skill, not promise verbatim phase-by-phase execution. Done: now names the invariant and safety gates, and explicitly says to propose only the phases needed rather than a fixed step order.
- [x] 8.3 `.claude-plugin/plugin.json`: bump `version` past `0.2.1` per the repo's release convention. Leave `description`/`keywords` untouched — that trim is the separate `trim-skill-metadata` proposal. Done: bumped to `0.3.0` (minor, restructure-sized); `description`/`keywords` untouched.

## 9. Verification

- [x] 9.1 `jq empty plugins/session-finalise/.claude-plugin/plugin.json` and `claude plugins validate plugins/session-finalise` both pass. Done: both ran clean (`plugin.json OK`; `✔ Validation passed`).
- [x] 9.2 End-to-end run: in a scratch repo, create uncommitted work, a stale/merged worktree, and an unsaved durable fact (something worth persisting that no memory file yet records). Run `/session-finalise` and confirm: every mutating step (commit, push, tracker update, delete, worktree removal) still requires explicit confirmation; commit still never targets `main`; save/commit still happens before anything destructive; the memory phase reconciles (proposes the new fact, doesn't recite a schema) rather than skipping it. Done in `/tmp/session-finalise-e2e.*` (removed after): commits landed on `develop` never `main`; the seeded stale fact (false npm claim) was correctly flagged via disk reconciliation and corrected; a new fact was authored by copying the existing file's frontmatter shape. Scratch fixture deleted afterward.
- [x] 9.3 During the same run, deliberately exercise at least one reference-only mechanic (e.g. the cleanup phase's "remove the worktree **and** its corresponding Claude Code project dir" rule) to confirm the router actually pulls in and applies `references/` content rather than the split producing a reference that never gets read. Done: removed the merged scratch worktree and confirmed its corresponding `~/.claude/projects/<slug>/` dir was removed alongside it, per `references/cleanup.md`.
- [ ] 9.4 Commit the change on a branch off `develop` (never `main`), one logical commit per section above where practical; open a draft PR per the repo's git workflow. Not done by this agent — instructed to run no state-mutating git commands; the calling session handles commit/PR.
