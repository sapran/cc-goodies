---
description: Run the end-of-session finalise checklist.
argument-hint: [optional notes — e.g. "skip cleanup" or "write a handoff"]
---

# /session-finalise

Use the **session-finalise** skill to wrap up the current work session. Follow that skill's phased checklist exactly, including its safety gates (confirm before any commit, push, file deletion, or worktree removal; never commit to `main`).

Additional instructions from the user for this run (may be empty): **$ARGUMENTS**
