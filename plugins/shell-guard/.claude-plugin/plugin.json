{
  "name": "shell-guard",
  "version": "0.4.1",
  "description": "Block or ask before a small set of catastrophic shell commands run (rm -rf / or ~, dd/redirect to a disk, mkfs, fork bombs, curl|sh, chmod 777, eval, sudo, reboot/shutdown). Catches plain-form accidents; fail-open, configurable.",
  "author": {
    "name": "Volodymyr Styran",
    "url": "https://github.com/sapran"
  },
  "license": "MIT",
  "keywords": ["shell", "hooks", "safety", "dangerous-commands", "pretooluse"],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/shell-guard.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
