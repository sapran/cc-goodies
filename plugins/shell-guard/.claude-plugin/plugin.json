{
  "name": "shell-guard",
  "version": "0.2.0",
  "description": "Block a curated set of dangerous shell commands (rm -rf / or ~, dd to a disk, mkfs, fork bombs, curl|sh, file truncation, chmod 777, eval, sudo, reboot/shutdown) before they run. Covers a typical shell deny list; fail-open, configurable.",
  "author": {
    "name": "Volodymyr Styran",
    "url": "https://github.com/sapran"
  },
  "license": "MIT",
  "keywords": ["shell", "hooks", "safety", "dangerous-commands", "pretooluse"],
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/shell-guard.sh",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
